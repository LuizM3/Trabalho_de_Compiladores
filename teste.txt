   // Declaração e atribuição de variaveis
    int x;
    x = 10 // Faltando ";"
    x = -4 + 5 - 3;

    // Estrutura condicional composta
    if ((x - -u) > 2 | y != z & z >= 12){
        int k;
    // Faltando "}"

    else {
        read (h p); // Faltando ","
        int a;
        print (x, y, 4, l);
    }

    // Laço de repetição
    while(x > 3){
        if(g == 0){
           int l;        
        }
    }
    int j;
 